version: '2'
services:
  gender:
    restart: always
    build:
      dockerfile: Dockerfile
      context: .
    ports:
      - "5000:5000"

    environment:
      - VIRTUAL_HOST=gender-recognition.kevinmouofo.com
      - LETSENCRYPT_HOST=gender-recognition.kevinmouofo.com
    
